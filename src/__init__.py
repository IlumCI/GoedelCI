"""The example project. Delete this directory and point `goedel.toml` at yours.

It exists so that `ci` is green the moment this template is cloned, and so
that the loop has something real to practise on before it is pointed at
anything that matters. Everything here is deliberately small enough to read in
one sitting and deliberately incomplete enough that a milestone can be written
against it.
"""
